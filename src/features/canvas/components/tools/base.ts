/**
 * Base types and classes for canvas tools
 */

export interface Vector2d {
  x: number;
  y: number;
}

export interface BaseCreationTool {
  // Base interface for creation tools
  // Currently used as a type import but functionality is implemented directly in tools
}